from django.db import models

# Create your models here.
# class faculty_details(models.Model):
    # faculty_name=models.models.OneToOneField("app.Model", verbose_name=_(""), on_delete=models.CASCADE)(max_)
